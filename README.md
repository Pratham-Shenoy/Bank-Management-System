# Bank-Management-System
A simple prototype of how a banking system works :)
The vision it to scale and optimize (this being a crud application, doesn't have a lot of scope for optimization though) it as far as I can.